---
name: verification-responsive
description: >
  Shim Vibe — vérifier le rendu responsive AVANT de pousser toute modification
  frontend (composant, CSS, layout, template de section). À utiliser dès qu'une
  PR touche `components/`, `app/**/*.css`, `app/page.tsx` ou une page statique de
  `public/` : dérouler la checklist des viewports (desktop cibles, frontières de
  breakpoints, mobile 375), tester le débordement horizontal et TOUS les états
  dynamiques du composant, puis remplir la case « Vérification responsive » du
  template de PR. Délègue à la skill Claude Code canonique du même nom.
  Déclencheurs : « vérifie le mobile », « responsive », « breakpoint », « ça
  déborde », toute retouche de globals.css ou d'un composant de section.
---

# verification-responsive (shim Vibe)

La procédure canonique vit dans la skill Claude Code de ce repo. Ne pas la
reproduire ici — toute duplication diverge.

**Lis et suis :** `.claude/skills/verification-responsive/SKILL.md` (chemin
relatif à la racine du repo `vitrine-showcase.github.io`).

En cas de conflit avec `AGENTS.md`, `AGENTS.md` prime.
