name = "vitrine-api"
main = "src/index.ts"
compatibility_date = "2026-08-01"
workers_dev = true

# Le pilote Neon parle HTTP, pas TCP : pas besoin de nodejs_compat ni de
# Hyperdrive. C'est ce qui rend l'API déployable sans autre infrastructure.

# JOURNAUX CONSERVÉS. Sans cette section, le Worker n'en garde AUCUN : la
# seule façon d'observer quoi que ce soit est `wrangler tail`, qui n'écoute
# qu'en direct. Le 2026-08-26, une panne installée depuis des SEMAINES — quatre
# tables absentes de Postgres, donc des échecs à chaque passe, donc les Deploy
# Hooks retenus en permanence — n'a pu être diagnostiquée qu'en attachant un
# `tail` puis en attendant qu'un cron veuille bien se déclencher. Une panne
# qu'on ne peut regarder qu'au moment où elle se produit est une panne qu'on ne
# corrige pas.
#
# `head_sampling_rate = 1` : on garde TOUT. Le volume est de quelques dizaines
# de requêtes par heure, pas de quoi échantillonner, et c'est précisément la
# requête rare — la tranche de synchro qui échoue une fois sur six — qu'un
# échantillonnage ferait disparaître.
[observability]
enabled = true
head_sampling_rate = 1

[vars]
# Durée de cache des réponses, calée sur le cycle de rafraîchissement des
# données (4 h). C'est la pièce qui rend l'API insensible à la charge :
# Postgres ne voit qu'une poignée de requêtes par fenêtre, que l'API serve dix
# clients ou dix mille.
CACHE_TTL_SECONDS = "14400"

# Identité de l'administration. Ces deux valeurs ne sont pas des secrets — le
# `aud` figure déjà dans l'URL de redirection d'Access — mais elles sont
# INDISPENSABLES : sans elles, /admin refuse de répondre, par choix.
ACCESS_TEAM_DOMAIN = "capp-vitrine.cloudflareaccess.com"
ACCESS_AUD = "8217d6f3af04da4d6185d6ba20db7c1654b16ad545010eb8f84dddd9ffe1e219"

# Sync DIRECT Athena (chaîne émancipée de GitHub, cron de la minute :10).
# BASCULE 2026-08-19 : "true" = les Deploy Hooks partent quand LES 15 tables
# ont réussi (tout ou rien). Retour à l'ombre : remettre "false".
SYNC_TRIGGER_DEPLOYS = "true"
# Coordonnées Athena (non secrètes) : base Glue des datamarts DEV et
# compartiment de résultats, les mêmes que ceux que `tube` résout.
ATHENA_REGION = "ca-central-1"
ATHENA_DATABASE = "gluestackdatamartdbd046f685"
ATHENA_OUTPUT = "s3://pipeline-stack-athenaqueryresultsbucket6f63bbe4-1hrrrojv867l3/"

# L'illustration de la Une des unes (routes /v1/art, src/art.ts). Le bucket
# est créé une fois : npx wrangler r2 bucket create vitrine-art
#
# CE BUCKET SERT AUSSI L'INSTANTANÉ DES DONNÉES, sous le préfixe
# `data/snapshot/` (routes /v1/snapshot, src/snapshot.ts). Un bucket dédié
# aurait été plus propre à lire, mais il aurait fallu le créer AVANT le
# premier `wrangler deploy` : un binding dont le bucket n'existe pas fait
# échouer le déploiement. ART_BUCKET est déjà lié et déjà éprouvé — une
# ligne de préfixe coûte moins qu'un déploiement raté.
[[r2_buckets]]
binding = "ART_BUCKET"
bucket_name = "vitrine-art"

# L'orchestrateur du cron s'appelle lui-même par tranches (/v1/sync-athena).
# Un fetch() vers son PROPRE nom d'hôte ne revient JAMAIS au Worker —
# Cloudflare coupe la boucle — et c'est pourquoi la passe autonome n'a laissé
# aucune trace sur six occurrences le 2026-08-19, pendant que les passes
# pilotées de l'extérieur réussissaient. Le service binding vers soi-même est
# le chemin sanctionné : chaque appel est une invocation NEUVE, avec son
# budget CPU à elle — exactement ce que l'orchestration par tranches cherchait.
[[services]]
binding = "SELF"
service = "vitrine-api"

# Secrets à poser hors du dépôt :
#   wrangler secret put DATABASE_URL
#   wrangler secret put AWS_ACCESS_KEY_ID_DEV      (clé de LECTURE Athena)
#   wrangler secret put AWS_SECRET_ACCESS_KEY_DEV  (idem)
#   wrangler secret put SLACK_WEBHOOK_URL          (optionnel, alertes)
#   wrangler secret put DEPLOY_HOOK_PROD           (à la bascule)
#   wrangler secret put DEPLOY_HOOK_DEV            (à la bascule)
#   wrangler secret put SYNC_INTERNAL_TOKEN       (orchestration du cron)
#   wrangler secret put SNAPSHOT_TOKEN            (lecture de l'instantané par le build)
#
# SNAPSHOT_TOKEN : jeton que le build présente pour lire /v1/snapshot. Comparé
# ICI, dans le Worker, et jamais dans la table des clés de Postgres — c'est ce
# qui rend le chemin du build insensible à une base indisponible, ce qui EST
# le correctif. Poser la MÊME valeur que la variable VITRINE_API_KEY déjà
# configurée sur les deux projets Pages : la bascule ne demande alors aucune
# variable supplémentaire côté Pages.
#
# Tant que ce secret est absent, /v1/snapshot répond 503 et ne sert rien :
# fermé par défaut, parce que les jeux de données sont le produit.

# Recharge Postgres depuis les JSON publiés.
#
# HORAIRE VISÉ : 02:00, 06:00, 10:00, 14:00, 18:00, 22:00 HEURE DE NEW YORK,
# fixe toute l'année, sans intervention.
#
# Pourquoi 02:00 et pas minuit : refresh-data.yml part à :50 et dure plusieurs
# minutes — Athena, illustration, musique, commit. On lit 70 minutes plus tard,
# une fois les données atterries.
#
# POURQUOI DOUZE HEURES POUR SIX EXÉCUTIONS : le cron Cloudflare est en UTC, et
# UTC ignore l'heure avancée. On enregistre donc les deux jeux — 2,6,10,14,18,22
# (valables l'été) et 3,7,11,15,19,23 (valables l'hiver) — et `isTargetHourInNY`
# dans src/sync.ts laisse passer seulement ceux qui tombent sur une heure visée
# à New York. Les six autres ressortent immédiatement.
#
# L'alternative était de changer cette ligne deux fois par an. Un rappel
# semestriel finit toujours par être oublié, et l'oubli est silencieux.
[triggers]
# BASCULE 2026-08-19 : un seul cron, la chaîne émancipée (minute :10 dans
# src/index.ts) : sync DIRECT Athena -> Postgres, heures visées
# {0,4,8,12,16,20} à New York, deux jeux UTC été/hiver, garde-fou
# isAthenaTargetHourInNY (src/schedule.ts).
#
# Le cron :00 (JSON publiés -> Postgres) est RETIRÉ, pas seulement dormant :
# refresh-data.yml ne tourne plus qu'une fois par semaine, et recopier ces
# JSON vieillissants écraserait les données fraîches du raffineur toutes les
# 4 h. Le code du chemin JSON reste (branche minute :00 de src/index.ts et
# route /v1/sync) : retour arrière = remettre la ligne de cron ci-dessous.
#   "0 2,3,6,7,10,11,14,15,18,19,22,23 * * *"
crons = [
  # :56 de l'heure qui PRÉCÈDE l'édition — la passe qui vise l'heure pile.
  # (#570 : à :10, l'édition du midi n'était en ligne que vers 12h18.)
  "56 0,3,4,7,8,11,12,15,16,19,20,23 * * *",
  # Filet, 14 min plus tard : rattrape les cycles où la cascade des raffineurs
  # avait pris du retard et n'avait rien publié à :56. Sans danger depuis #582
  # (un second appel de hook reçoit 304, qui n'est plus traité comme un échec).
  "10 0,1,4,5,8,9,12,13,16,17,20,21 * * *",
]
